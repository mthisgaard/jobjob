<%= simple_form_for [job, task], html: { class: "form-inline mt-2" } do |f| %>
  <%= f.input :title, label: false, :placeholder => 'Add a task...' %>
  <%= f.submit "Add", class: 'btn btn-primary' %>
<% end %>